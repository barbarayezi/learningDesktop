# 2026-08-21 工作日志

## W1 视频打散（用户反馈“一连三天看同一个视频”）
- 现象：W1（8.15–8.21, focus 数据治理）`vWorkVideos` 把同一 `U.c3`（北大青鸟·CDGA 17章精讲 第3讲）穿了精读/二刷/笔记/脑图三个动作，按「工作日轮询」渲染后周二、周四、周五都落到同一集，UI 上像连续三天看一个视频。
- 修复（commit `cfcb03c`）：W1 工作日的 5 个视频改为互不相同的 URL —— 周一 Ch1(U.c1)、周二 Ch3(U.c3)、周三 数语#3(U.b3)、周四 Ch2 伦理(U.c2)、周五 Ch4 架构(U.c4)；同步更新 `vWork`/`vWeekend` 文案。Ch3 的巩固（二刷+案例卡）保留在 `vWeekendVideos`，不再占工作日。
- 机制提醒：每日卡的视频是 `pool[wi % pool.length]`（wi=周一0..周五4）轮询；**只要某周 `vWorkVideos` 里同一 URL 出现 ≥2 次，就一定会有若干天重复显示该集**。今后编排工作周如有意反复看某章，建议把重复动作集中到周末，或显式用不同 URL（不同分P/不同 UP）区分。
- 生效前提：本机 Mac Mini 跑的 `proxy_server.py` 仍是旧代码，需在其上 `git pull` + 重启（launchctl unload/load 或 kill 进程）后刷新 localhost:8080 才能看到新安排。

## 公网自动部署（GitHub Actions）
- 用户确认要消除“改了 GitHub 公网没变”的落差，让我配自动部署。查证公网站点是 CloudBase 静态托管、与 GitHub 原无自动同步（之前只能手动 deploy）。
- 建 `.github/workflows/deploy.yml`（commit `5e9932c`）：`push` 到 main 且命中 `index.html`/`资料/**`/`articles.js`/工作流本身时，用 `TencentCloudBase/cloudbase-action@v1` 自动部署；envId 硬编码 `test-d5gf0o9ky7d34beaf-1469471831`。
- 关键安全处理：部署前 `rm -rf` 掉 `.git/.github/.workbuddy/proxy_server.py/cloudbase-sync-proxy/landing-page/Find/overview.md/node_modules/资料/harvest_articles.py`，避免含 service_role 密钥的后端文件上公网；并把 `资料/articles.js` 复制到根 `articles.js`（HTML 引用的是根目录版）。
- 待办（用户侧）：GitHub 仓库 Settings→Secrets→Actions 加 `TCB_SECRET_ID`、`TCB_SECRET_KEY`（腾讯云 CAM 密钥）。首次 push 触发的工作流会因缺密钥失败，配置后重跑/再 push 即生效。
- 18:09 用户确认公网仍未更新。实测：curl 公网 HTML，新版特征 `Ch2 数据处理伦理` 出现 0 次，旧版特征 `Ch3 数据治理（笔记/脑图）`/`Ch3 数据治理（二刷）` 各 1 次 → 公网停旧版。根因：工作流已就位但缺 CAM 密钥，deploy 步骤无凭证失败，未上传。用户配好 Secrets 后需 Re-run 失败记录或再 push 一次。
