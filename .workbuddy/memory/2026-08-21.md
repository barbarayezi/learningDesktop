# 2026-08-21 工作日志

## W1 视频打散（用户反馈“一连三天看同一个视频”）
- 现象：W1（8.15–8.21, focus 数据治理）`vWorkVideos` 把同一 `U.c3`（北大青鸟·CDGA 17章精讲 第3讲）穿了精读/二刷/笔记/脑图三个动作，按「工作日轮询」渲染后周二、周四、周五都落到同一集，UI 上像连续三天看一个视频。
- 修复（commit `cfcb03c`）：W1 工作日的 5 个视频改为互不相同的 URL —— 周一 Ch1(U.c1)、周二 Ch3(U.c3)、周三 数语#3(U.b3)、周四 Ch2 伦理(U.c2)、周五 Ch4 架构(U.c4)；同步更新 `vWork`/`vWeekend` 文案。Ch3 的巩固（二刷+案例卡）保留在 `vWeekendVideos`，不再占工作日。
- 机制提醒：每日卡的视频是 `pool[wi % pool.length]`（wi=周一0..周五4）轮询；**只要某周 `vWorkVideos` 里同一 URL 出现 ≥2 次，就一定会有若干天重复显示该集**。今后编排工作周如有意反复看某章，建议把重复动作集中到周末，或显式用不同 URL（不同分P/不同 UP）区分。
- 生效前提：本机 Mac Mini 跑的 `proxy_server.py` 仍是旧代码，需在其上 `git pull` + 重启（launchctl unload/load 或 kill 进程）后刷新 localhost:8080 才能看到新安排。

## 公网自动部署（GitHub Actions）
- 用户确认要消除“改了 GitHub 公网没变”的落差，让我配自动部署。查证公网站点是 CloudBase 静态托管、与 GitHub 原无自动同步（之前只能手动 deploy）。
- 建 `.github/workflows/deploy.yml`（commit `5e9932c`）：`push` 到 main 且命中 `index.html`/`资料/**`/`articles.js`/工作流本身时，用 `TencentCloudBase/cloudbase-action@v1` 自动部署；envId 硬编码 `test-d5gf0o9ky7d34beaf-1469471831`。
- 关键安全处理：部署前 `rm -rf` 掉 `.git/.github/.workbuddy/proxy_server.py/cloudbase-sync-proxy/landing-page/Find/overview.md/node_modules/资料/harvest_articles.py`，避免含 service_role 密钥的后端文件上公网；并把 `资料/articles.js` 复制到根 `articles.js`（HTML 引用的是根目录版）。
- 待办（用户侧）：GitHub 仓库 Settings→Secrets→Actions 加 `TCB_SECRET_ID`、`TCB_SECRET_KEY`（腾讯云 CAM 密钥）。首次 push 触发的工作流会因缺密钥失败，配置后重跑/再 push 即生效。
- 18:09 用户确认公网仍未更新。实测：curl 公网 HTML，新版特征 `Ch2 数据处理伦理` 出现 0 次，旧版特征 `Ch3 数据治理（笔记/脑图）`/`Ch3 数据治理（二刷）` 各 1 次 → 公网停旧版。根因：工作流已就位但缺 CAM 密钥，deploy 步骤无凭证失败，未上传。用户配好 Secrets 后需 Re-run 失败记录或再 push 一次。

## DAMA-DMBOK 一页纸 PDF「不见了」—— 真因是 `资料/` 整目录从未上云（18:47 起）
- 用户截图：腾讯智能体弹出 PDF 阅读器，请求 `资料/免费备考/dmbok-overview.pdf` → 404 NoSuchKey（Key 解码后是「资料/免费备考/」不是「资料/03_机构免费资料/」）。
- **真因不在请求 URL**：实测枚举公网 `资料/` 子目录 13 条路径 → **全部 404**（含 README.md / articles.js / 两个 PDF / 三个 free html / 笔记 md）。公网只有 `index.html` 和根 `articles.js` 两个文件。`资料/articles.js` 都不存在 → 不是单 PDF 缺失，是整目录从部署就被吞了。
- 验证本地：`find 资料/` 12 个文件齐；`git ls-files 资料/` 全跟踪；最近 4 个 commit 没碰这些文件。仓库健康。
- 查 GitHub Actions API：run id `32467059162` (5e9932c 触发的) `conclusion=failure`、step 5「Deploy static to Tencent CloudBase」0.4 秒内挂掉、无子步骤错误信息 → **典型 secrets 未注入导致 cloudbase-action 鉴权失败**。后续没自动重跑。
- 已做：commit `cd89c6b` 改「文档版本 v1→v1.1」+ 几个 doc/landing-page 一并 stage，触发第二次运行（run id `32474603130`）。结论：同样姿势 step 5 0.4 秒 fail，**说明 Secrets 仍没注入**（用户以为自己配好了但实际没点保存 或 勾选的是其他命名）。
- **待 user 做**：去 https://github.com/barbarayezi/learningDesktop/settings/secrets/actions 确认能看到 `TCB_SECRET_ID` 与 `TCB_SECRET_KEY` 两条；然后点 https://github.com/barbarayezi/learningDesktop/actions/runs/32474603130 页面右上「Re-run failed jobs」即可。
- 失败的现场信息只能从那条 run 的 Step 5 折叠日志看（GitHub API 拉 logs 需要 admin 权限，匿名 webfetch 拿不到），让用户手动去看一眼「Deploy static to Tencent CloudBase」step 的展开内容，那里会写明「SecretId is required」之类明确提示。
- **重要提醒**：仓库 `repository.private: false`（public）。`TCB_SECRET_ID`/`TCB_SECRET_KEY` 是账号级 API 密钥（不能控制 IAM 子账号），建议配对前先去 CAM 生成「专给 CloudBase 的子账号」+最小权限策略；万一密钥泄露被滥用可去 https://console.cloud.tencent.com/cam/capi 一键 disable。
- **流程改进**（下次不要再栽这个跟头）：
  1. 任何配 Actions 自动部署的 commit，必须在同一次工作回合末尾观察 Actions API `conclusion=success` 才算交付完成；
  2. 记忆里只写「已配好」是认知陷阱，得写「已配好 + 第一次跑成功」。工作记忆里写「现已配 GitHub Actions 自动部署」却没在 Actions 页确认绿勾，属于已知坑。
  3. 后续考虑在 deploy.yml 加一个 `Pre-check secrets` step：`if [ -z "${{ secrets.TCB_SECRET_ID }}" ]; then echo "❌ TCB_SECRET_ID missing"; exit 1; fi`，把 secrets 缺失从 0.4 秒神秘 fail 变成显式红字日志，调试友好。
