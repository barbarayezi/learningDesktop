# 2026-09-03 工作日志

## 刷题「首题是已答题 / 跳题」修复（已上线）
- 现象：点每日中心「刷题」进入后，首题常为已答过的题（直接显示答案+解析）；答完一题跳到下一道、看不到本题反馈。
- 根因：上次 git 仓库损坏恢复（`reset --mixed FETCH_HEAD`）把工作区 `index.html` 回退到旧版本，导致「去掉 `_listKey` 里的 `answeredCount`」这一修改未真正生效。`_listKey` 仍含 `answeredCount` → 答一题即让缓存 key 变化、列表重算、curIdx 错位。
- 修复（`index.html`，提交 `3c097ec` 已 push，Actions success 已部署公网）：
  - `_listKey` 刻意不含「已答集合」：一轮刷题期间 list 固定，答一题不重排。
  - `openQuiz` 从每日中心/题库卡进入时统一 `_invalidateListCache()`，按最新已答状态重排。
  - mode-toggle 切回刷题模式时清缓存 + `currentIdx/finished` 归零，避免命中旧 list。
- 验证：Node vm + DOM stub 跑真实题库（247 题），5 项断言全过（进入后前5题已答数=0、答一题后顺序不变、点下一题落原第2题、all域优先未答）。

## 09:25 boot 队列架构改造（任设备自动同步，零手动操作）
- **用户原话**：「能不能确保无论我到哪台电脑上打开这个网页，数据都是同步一致的？不要有任何让用户强算之类的操作」
- **根因**：原 initCloud 是 IIFE 立即执行 + 完成后 1.2s 强制 reload，UI 渲染走在云端拉取之前，依赖 reload 才能看到完整数据。
- **改造方案**：
  1. body 开头加 `#bootSplash` 全屏遮罩（z-index 99998，含 ☁️ 图标 + "正在同步学习记录…"）
  2. `(async function initCloud(){...})()` 改 `async function initCloud(){... return bool}`，去掉 IIFE 立即执行和 reload 段（didChange 变量也删）
  3. 引入 boot 队列：`_bootTasks[]` + `function boot(label,fn){...}` 替代顶层 `safe('...')`（19 处），延迟到 initCloud 完成后执行
  4. boot 队列最前插入「存储快照重载」：重新 load 5 个 `let X = loadXxx()` 模块级快照（dailyChecks/notes/daynotes/videoWatched/wbSubMode），这些在脚本顶层立即赋值（merge 前=空），是热力图等用旧值的根因
  5. 文件尾 `(async function bootAll(){ await Promise.race([initCloud(), timeout 7000ms]); ... }())`：7s 超时兜底（云端失败按本地数据渲染）、splash 0.3s 淡出
  6. 独立 15s setTimeout 兜底（主脚本异常时强制隐藏 splash，不至于页面永远盖住）
- **CI 验证**（headless dump-dom + console）：
  - splash display:none ✓
  - 19 模块全部 OK（console 打印 "✅ 页面渲染完成，共执行 19 个模块"）
  - initCloud 21 条数据同步 ✓
  - 热力图 hl-done=19（云端 18 天打卡 + 今日）✓
  - 勾选 chk-row done=8（云端 bb-cdga-check 8 个 true）✓
  - 错误红条 0 ✓
  - 5 条 W3 锚点式拆解正确渲染 ✓
- **commit 39f0104 + push**（3c097ec..39f0104），公网已生效（curl 验证 bootAll/存储快照重载 都已上线）

## 跨设备自动同步架构最终形态
- 任何设备打开网页 → 0.3-1s 内看到 bootSplash（"正在同步学习记录…"）→ initCloud 拉 Cloudflare Worker → 深合并 localStorage → boot 队列渲染 UI → splash 淡出 → 完整内容
- **零手动**：无需 Cmd+R、无需 Cmd+Shift+R、无需清缓存、无需「强制刷新」
- **零闪烁**：没有「先空再 reload 再满」的视觉跳变
- **失败兜底**：云端 7s 超时按本地数据渲染、15s 终极隐藏 splash

## 09:30 「如何确保不再复发」—— 三道防线落成
起因：用户问「如何确保以后不会再产生这类问题」。结论：根子不在那段代码，在流程。

**① 回归测试固化（tests/regression.js，零依赖，34 项断言，已绿）**
- 提取 index.html 内联脚本 → vm + DOM stub 干跑 → 断言关键行为：
  语法可加载 / boot 队列被 bootAll 消费 / 刷题列表优先未答且一轮内不重排 /
  错题本 Leitner 升降盒与毕业 / 错因可标注。
- **受控崩溃测试已做**：临时注释 `bootAll();` 跑一遍 → 测试报错（证明不是永远绿的假警报），随后恢复。
- 规则写入 MEMORY.md：改完 index.html 必须跑它，绿灯才允许 commit；新增断言加进这里，不再写用完就删的临时脚本。

**② CI 质量闸门（deploy.yml）**
- checkout → setup-node → **`node tests/regression.js`（失败即 exit 1 中止）** → 补根目录 articles.js → rm -rf（含 tests/，绝不上公网）→ tcb 部署。
- 公网永远拿不到坏代码。paths 已含 `tests/**`，改测试也触发重跑。

**③ 流程铁律（MEMORY.md · 改动交付铁律，最高优先级）**
- 四道防线：回归测试唯一验收标准 / CI 闸门 / git 危险操作后复核改动是否还在 / "已修复"措辞门槛（测试绿灯+Actions success+公网双向断言三者齐备才说修好）。
- 直接针对 9-03 翻车：git 危险操作（reset/checkout --/stash pop/仓库重建/pull --rebase）后，必须 `git diff HEAD -- index.html` + grep 关键特征，确认自己的编辑没被回退再继续。

## 10:00 全量交付核验（用户追问"以上确定都完成了吗"）

**新增最强验收手段：三方哈希比对**（应写进「已修复」验收流程，优于任何特征串断言）
```
md5sum index.html                     # 本地工作区
git show HEAD:index.html | md5sum     # 已提交版本
curl -s <公网>/index.html | md5sum    # 线上版本
```
本次三者全等 `7b71084e84df96c4e3b015c6a36328f3`（586748 B）。
grep 只能证明"某字符串存在于公网"，哈希相等才能证明"我改的那份就是线上跑的那份"——
这正是 8-21/9-03 两次「假修复」漏检的根本原因。

**核验远程仓库文件内容的正确姿势**：`raw.githubusercontent.com` 在本沙箱被拦
（HTTP 200 但 body 为空），`api.github.com/contents` 会 403 限流。
**改用 `git show origin/main:<path>`** —— `origin/main` 是 fetch 下来的远程真实对象，
比 curl 可靠且不受限流影响。据此确认远程 deploy.yml 第 33/35 行含
`Regression tests (quality gate)` + `node tests/regression.js`、无 `continue-on-error`，
paths 含 `tests/**`，rm -rf 清单含 `tests`（与公网 `/tests/regression.js` → 404 一致）。
`git rev-parse origin/main:tests/regression.js` == `git hash-object tests/regression.js` = `1141a35`。

**教训：断言用错特征串会造成虚警。** 本次先用 `wb-review-today`、`BOX_DAYS` 断言错题本，
公网计数 0，一度误判漏部署；回查本地也是 0 —— 是我记错了类名/常量名。
**正确做法：断言前先在本地 grep 确认该特征真实存在**，否则"公网=0"无法区分
"漏部署"与"特征串写错"。错题本真实特征为 `cdga-wrong-book-v1` / `function addWrong` /
`function wrongTodayList` / `quiz-wrongbook`（本地公网均一致）。

**收尾**：`.gitignore` 补 `.workbuddy/automations/`（本地自动化配置，机器相关），
工作区现已完全干净。最终提交 `b65776e`，本地 == origin/main。
Actions 近 3 次运行（3087a9c / 39f0104 / 3c097ec）全部 `completed / success`。

---

## 回归测试补「题库数据体检」+「答题推进边界」（提交 `ea814fb`）

用户追问「能确保以后不会再发生这种题目的 bug 吗」，逼出一个真实盲区：
原有 34 项断言**只覆盖列表逻辑**，完全没覆盖「题目本身的 bug」——
答案字母越界 / 选项重复 / 题干重复这类问题**不会让页面报错**，
只会让人做题时看到错的东西，逻辑测试永远抓不到。

新增 26 项，共 **60 项断言**：
- **组6 题库数据体检**（CDGA 247 题 + `RK_QUIZ` 软考题库逐题）：字段完整 /
  选项为数组且 ≥2 项无空串 / 同题内无重复选项 / **答案字母必须落在选项范围内** /
  每题有解析 / 无重复题干 / 每域题目数 ≥5（够抽一轮 pickSize=5）。
- **组6 解析质量棘轮**：`NO_EXPLAIN_BASELINE = 91`（实测存量，247 题里 91 题的 `e`
  只有出处署名、无任何讲解）。**只许降不许升**，超基线即 exit 1，防止新加题不写解析；
  存量用 `warn()` 打 ⚠️ 但不阻断部署（避免为了绿灯而阻塞正常改动）。
- **组7 答题推进与边界**：一轮题量 = pickSize 不缩水 / 逐题作答+`renderPractice`
  全程不抛错 / 全答完列表不塌缩 / `currentIdx` 越界(999)与负值(-5)都被修正且不崩 /
  重置后清空并能重新出题 / 切换域后列表只含该域题目。

**首跑即抓出一个真实错题**（这正是加这组断言的价值）：
第 77 题「关于逆向工程」选项 C、D **一字不差重复**（`概念数据建模是第一步…`），
做题时会出现两个相同选项。已改 D 为独立干扰项（`物理数据建模是最后一步…`），
并补上原本缺失的解析（逆向工程自底向上：物理→逻辑→概念）。

**踩坑：长度阈值判"有没有解析"会误伤。** 初版用「剥掉署名后剩余 <8 字符即无解析」，
把 `全生命周期管理。`（7 字，真解析）误判为欠债，且把非题目对象的 `e` 字段
（`inspired-public` / `datafield` / `高分精学` 等资料条目）一起算进来 → 报 100 而非 91。
**正确做法：按 `·｜|` 切段，丢掉匹配 `网友回忆版|CC BY-SA|weixin_\d+|csdn*|答案推断` 的
纯署名段，一段不剩才算无解析**，且只遍历 `QUIZ` 数组、不 grep 全文件。

**受控崩溃测试**：临时把首题 `a:'B'` 改成 `a:'Z'` → 「答案字母落在选项范围内」
报红 + exit 1，证明新断言不是永远绿的假警报；随后恢复并确认 `grep -c "a:'Z'"` = 0。

**验收（三证齐备）**：60 项绿灯 → Actions `ea814fb` `completed/success` →
三方哈希全等 `899d69e875c8fb62328a13f3e766605e`，公网重复选项串计数 = 0。

---

## 11:20 记忆卡片（每周关键概念）盲测呈现优化（本地改动，待部署）
- 用户截图反馈：每日学习中心「本周 W3 · 数仓BI + 主数据」卡片答案直接裸露，记忆卡片失去盲测意义。
- 结论：当前代码其实已默认 `hidden` 折叠答案（6327 行 `<span class="dc-concept-a" hidden>`），
  截图里答案展开是用户已点过展开按钮或浏览器缓存旧版；但呈现设计仍有 3 处可改。
- 改动（index.html，未 commit）：
  1. 清单题面改提问式（去结论外露）：Inmon 自上而下 / 分层归属 Kimball / Inmon 四特征 三条
     `.chk-row span` 把结论从题面移入括号内答案，题面改为问句，强化主动回忆。
     不增删行、不改索引，符合清单数据模型铁律。
  2. 前端渲染去「答案：」粗体前缀：`conceptsHTML` 答案区不再渲染 `<b>答案：</b>`，
     改用 `.dc-concept-a` 左边框样式区分（227 行 CSS）。
  3. 加「收起答案」按钮：`[data-act="hide-c"]` 允许展开后重新闭卷自测，
     展开时隐藏「展开」按钮、显示「收起」按钮（6478-6501 行）。
- 验收：回归测试 60 项全绿；另用真实 `conceptPair` 函数对三条新文本验证，
  题面/答案均正确拆分且不含括号字符（hasAnswer=true）。
- 交付（用户授权「要」走全链路）：
  - commit `ba136f5` → push origin main（`7341555..ba136f5`）。
  - `git ls-remote origin main` 回 `ba136f5...` = 本地 HEAD，**远程已拿到改动（铁律③复核通过）**。
  - 本机无 `gh`/无 `GITHUB_TOKEN` 无法直接查 Actions conclusion；改用三方哈希替代（铁律"主证据"）：
    WORKSPACE(LF)=BLOB(HEAD)=PUBLIC=`973053a8f54e498e934ecbdf5aec37db`（588225 B 全等）。
    公网拿到新字节本身反证 Actions 跑过测试闸门并部署成功（否则公网仍是旧版、哈希对不上）。
  - 用户现在任意设备刷新即见新盲测卡片（提问式题面 + 无「答案：」前缀 + 可收起）。

## 补漏：Kimball 题面漏改「=」陈述句（用户截图复核抓出）
- 上午 ba136f5 改了 3 条题面（Inmon 自上而下 / 分层归属 / Inmon 四特征），**唯独漏了 Kimball 这条**，
  仍为 `Kimball｜自下而上 = 先零件后组装（维度建模：…）`——用 `=` 直接给答案，点开虚线框无新增信息。
- 用户 13:31 截图反馈「这个像是把答案放在题目里面了」→ AskUserQuestion 确认属于「题面泄露答案」。
- 改动（commit `42f33fa`，line 1601）：题面改 `数仓方法论｜Kimball 自下而上 的核心实现特点是`，
  `先零件后组装` 从题面移入括号内答案，与 Inmon 行结构对仗。索引不变，无需迁移。
- 验收：回归 60 项全绿；三方哈希 WORKSPACE=HEAD=PUBLIC=`70bdce2b…`（588225 B 全等）→ Actions 过闸门已部署。
- 教训：**批量改多条相似数据时，逐条对照原截图/原文核对是否有漏改项**，别只信「改了 3 条」的直觉。
  今后这类「同一模式多处」的改动，先在仓库 grep 一遍同模式未命中项（如再 grep `= 先` 确认无残留）。

## 15:42-16:20 修复：daily 卡片「优先未答」文案与 antiRepeat 实际状态脱钩
- **现场**：用户点每日卡片「刷题 数据仓库与BI 14/16·优先未答」→ 跳刷题页首题是**已答过的** #167（B 正确、带反馈）。
- **根因排查**：代码逻辑本身没错（antiRepeat=开 时首题必是未答，已用 sandbox 复现验证）。
  真正原因 = **用户在刷题页右上角关掉了「🆕 防重复」**（页内 btn-anti 翻转不清 answers），
  此后 list 从「全部题」随机抽 5 → 首题命中已做题。而 daily 卡片文案 `题 · 优先未答 · 每日抽 N`
  是**写死的**（index.html:6251），与实际 antiRepeat 状态无关 → 误导。
- **行为路径关键点**：`openQuiz` 在 antiRepeat=关 时会清空 answers + 换种子（by design）；
  但「页内切开关(btn-anti)/域chip/mode-toggle」**都不清 answers** → 用户答过的 14 题保留，
  却按随机模式抽题。4c 测试第一次用 openQuiz 模拟失败（answers 被清），改用 btn-anti 等效
  （翻转 + _invalidateListCache）才复现成功。⚠️ **以后模拟页内行为别用 openQuiz 入口**。
- **修复（a663ceb）**：① 按钮文案按 practice.antiRepeat 动态生成（开→优先未答/关→随机抽取）；
  ② 「防重复模式」提示双向化，关时明示「随机抽可能命中已做题 + 一键开启入口」；
  ③ regression 补 4b（文案不写死）/4c（防重复关行为定义 + 与开对照首题必未答）。
- 回归 60→67 项全绿后推送。

## 16:22 二次修复：daily card 入口强制开防重复（用户反馈首题仍是已做）
- 用户截刷题页（不是 daily card）：首题 #167 已答且 B 正确——核心痛点没解决。
- 反思：之前修复只把 daily card 文案改成动态化（"关→随机抽取"），没解决"从入口进入刷题页后首题仍是已做"的问题。文案改了但行为没动 → 用户体验脱钩。
- 根因复盘：用户从 daily card 进刷题（antiRepeat=true、answers 保留 14 题）→ 在刷题页右上角点 🆕 防重复按钮关掉（页内 btn-anti 翻转不清 answers）→ 切回该域 → practiceList 走 antiRepeat=false 分支从全部题抽 → 首题命中 #167。
- 修复（a0f0df4）：daily card 按钮 click handler 增加 antiRepeat=false 时**强制置 true** + savePractice，兑现"优先未答"承诺。用户仍可在刷题页手动关回。
- regression 4d 验证：handler 含强制开启 + 关→入口→开后首题未答。70 项断言全绿。
- 三方哈希 fb4b73d2 全等（公网=HEAD=本地）。
- **教训**：用户说"没修好"时立刻重新看截图——他在**刷题页**看到首题仍是已答，说明他要的不是"文案说清楚"，是"行为修正"。**先看用户最新截图判断他在哪里看到了什么**，比继续读代码逻辑更高效。
