# 2026-08-16

## 数据开发工程师证书注入学习计划
- 用户想在已有主线（CDGA 2027-03 → CDGP/CDAM → 上海交大在职博士 2028-09）上，加考「数据开发工程师」方向证书。
- 核实了证书现行状态：微软 DP-203 已于 2025-03-31 退役，替换为 DP-700；Databricks DE Associate 已更新 2026-05 版；阿里云 ACP 大数据仍是国内最对口中级证。
- 推荐：首选阿里云 ACP 大数据（开发方向，国内/国企 ROI 高）；国际工程背书选 Databricks DE Associate；职称加成加软考·数据库系统工程师。
- 容量结论：能装下。方案=CDGA 考后插 12 周密集块（2027-03→2027-06）考数据开发证，CDGP 从原 2027-04 顺延到 2027-06 启动。全程周投入不超 CDGA 阶段的 8–10h，无并行过载。
- 产出文件：`01_考证/数据开发工程师_证书推荐与排期.md`（含对比表、容量分析、mermaid 排期、行动清单）。
- 注意：DAMA 三级进阶规划文件里 CDGP 启动时间（原 2027-04）建议同步更新为 2027-06，保持两份计划一致。

## 用户确认（2026-08-16 补充）
- 数据开发工程师方向：**两张工程证都报，先阿里云 ACP 大数据，再 Databricks DE Associate**。
- 已执行：DAMA 三级规划中 CDGP 启动时间 2027-04 → 2027-06（时间轴 + 与 CDGA 衔接表两处同步），并在《数据开发工程师_证书推荐与排期.md》标 [x] 已确认。两份计划现口径一致。

## 每日学习中心 · 视频区 UI 收紧（2026-08-16 修复）
- W1 周末（2026-08-15 ~ 08-21）vWeekendVideos 原 3 项「6h 速成 + cv11 + 经验」与 vWeekend 文案「cv11 + 经验」不一致；6h 速成本属 vWork 范畴。删 ⭐ 6h 速成后，W1 周末剩 2 项：cv11 大纲(30min P1) + 一次过经验(12min P3) = 42min
- 截图诊断（用户报「看不清几条」）：cv11 实际渲染为 `📺 考纲专栏（cv11/12/13）` series box（Node 验过 HTML 含 data-skey="cv"），但 user 截图范围只覆盖 crash box，cv box 被默认折叠 / 视觉误读漏看
- 「▶️ 看课 / 视频」h4 加 IIFE 钩子动态显示「今天 N 项 · 共 Xmin」，CSS `.dc-h4-meta` 浅色胶囊 + `<b>` 高亮数字，**与各 series 内「今日 N 项 / 全集 X/3」互不冲突**——前者顶部总量，后者每系列进度
- git 嵌套 bug：learningDesktop/.git 在 git rev-parse 解析时被上层 01_Projects/.git 覆盖，AI 无法本地 commit，留给 user 手动 `git add index.html && git commit`

## 网站同步（2026-08-16 补充）
- 用户指出网站 http://192.168.103.37:8080/（= 本工作区 index.html，由 proxy_server.py 静态托管 + CloudBase 代理）。该站学习计划内容写死在 HTML 里，与 markdown 是两套独立副本，此前 markdown 改动未自动同步到网站。
- 已在 index.html「三级进阶总规划」页同步：时间轴插入「数据开发工程师证书 2027-03→06（ACP→Databricks DE）」、CDGP 改 2027-06→12（顺延）、实务沉淀顺延 2028-01→2028-06；衔接表拆为数据开发块 + CDGP 两行；加「2026-08-16 更新」callout。
- 网站尚无独立「数据开发工程师」详情页（详细排期仅在 markdown）。用户刷新浏览器即见更新（静态文件无需重启服务）。如需站内加独立页面可再做。

## 统一到网站 · 删除 md（2026-08-16 收尾）
- 用户最终决定：**今后不再写 md 计划，全部统一到网站**，且选择「彻底单一来源」= 删掉 01_考证、02_读博 下所有计划类 md。
- 已新增网站独立页 `data-dev`（数据开发工程师证书）：内容 = 原《数据开发工程师_证书推荐与排期.md》全量（结论/证书对比表/组合顺序/容量分析/协同/行动清单/风险），含可勾选 checklist（localStorage 持久化，仿 ladder 写法）。
- 已修三级进阶总规划页两处指向 md 的死链（1700、1801 行）→ 改为站内跳转 `showPage('data-dev')`；DAMA 页 doc-sub 去掉 md 引用；首页 hero 文案改为"单一来源，不再维护 markdown"。
- 已删除 11 份计划 md（01_考证 8 份 + 02_读博 3 份），内容已在网站完整覆盖；`资料/` 参考 md 保留。
- 验证：`grep "01_考证/"` 在 index.html 中已无残留，无死链。
- 待用户手动 git commit（index.html 改动）。

## 每日中心 · 分P视频展开 + 周末差异化（2026-08-16 修复 #2）
- 用户质疑：①视频内分P小视频（各自时长）被漏算；②15日与16日视频一样。
- ① 根因确属漏算：之前每个视频只标 1 个手写估算总时长，无分P/真实时长逻辑。实测 B站 `api.bilibili.com/x/player/pagelist?bvid=` 可达（沙箱有外网、无需登录），抓取全部视频：
  - **分P 视频（4 个，严重失准）**：b1 哈喽鹏程 39P/5h、b2 尚硅谷 19P/3.75h、whole 6h速成 38P/6.6h、whole2 15h完整版 49P/15.3h。我原标 60min 等，差 2.5~15 倍。
  - 单P 视频（c1~c17、b3~b9、exp）：时长估得基本准。
- 落地：
  - 内嵌 `VIDEO_PARTS`（4 分P视频的 `[{p,t,d}]`）+ `VIDEO_DUR`（全视频真实总秒数），数据自动抓取写入。
  - `videoRow` 改造：检测 bvid∈VIDEO_PARTS 时，总项加「▸ 含 N 个分P · 真实总时长 XhYm」徽章，用原生 `<details>` 展开每个分P（P序号+真实标题+真实时长+`?p=N` 单独链接），不单独勾选。
  - 加 `fmtDur(sec)` 工具（秒→XhYm/Xmin）。
- ② 周末差异化（用户选「仅周末两天差异化」）：`renderDay` 按 `d.wd==='周日'` 拆分——周六=`vWeekendVideos.slice(0,max(1,len-1))`、周日=`slice(1)`（单视频周周日变复盘日）。纯代码不改 WEEKS 数据。26 周验证：17 周有效差异 + 9 周本无视频 + 0 异常相同。
- 验证：vm 沙箱跑真实脚本，`renderVideoLinks(W24)` 确认生成 `<details>`/分P/真实时长；`DAILY_BY_ISO['2026-08-15'|'2026-08-16']` 确认 15=周六[cv11]、16=周日[经验] 不同。
- **未 commit**（git 嵌套 bug，同前）。改动全在 index.html：`VIDEO_PARTS/VIDEO_DUR` 插入、`fmtDur`、`videoRow`、`.dc-parts` CSS、`renderDay` 周末拆分。

## 软考报名提醒节点（2026-08-16 收尾）
- 用户确认把「软考·数据库系统工程师」排期设为：CDGA 后趁热冲刺（2027-03 → 2027-05），软考 2027 上半年报名（约 2027-03，考试约 5 月下旬）。
- 已在网站两段时间轴（三级进阶总规划页、数据开发工程师页）「CDGA 上场」之后、「⚡软考冲刺」之前，各插入一个独立提醒节点 `📌 软考报名（约 2027-03）`，琥珀色高亮，注明以 ruankao.org.cn 本省公告为准、通常 3 月开报、需提前备 APP 实名+属地证明。两页一致。

## CDGA 知识保温复习节点（2026-08-16 收尾）
- 用户担心 CDGA(2027-03) 与 CDGP(2027-08) 间隔 5 个月会遗忘知识点。确认：间隔确从最初 1 个月拉到 5 个月（因插了软考+数据开发证）。
- 缓解：两段网站时间轴「⚡软考(2027-03→05)」之后、「阶段3 数据开发证(2027-05→08)」之前，各插入紫色 `🧠 保温复习（2027-05 → 08）` 节点：每月 1 次 11 域速过（1–2h/月）+ 站内 spaced-repetition 复习提醒保持轮转，防工程证备考期遗忘 DAMA 基础。采用"轻量保温"而非"压缩 CDGP"，因用户选保留两张数据开发证完整备考期。

## 三个考证目标补成「每日级」学习计划（2026-08-16 收尾）
- 用户原问"刚加入的其他考证目标是否都拆分到每天"，澄清：此前只有 CDGA 有完整每日中心，软考/数据开发证/CDGP 仅到阶段级。用户选"三个全做"。
- 新建**通用每日中心框架**（参数化、独立于 CDGA、存储键隔离 bb-ruankao-/bb-datadev-/bb-cdgp-）：buildGenDaily(cfg) 生成 打卡热力图 + 周芯片 + 日期选择器 + 每日卡片(目标/看课/阅读/自测/打卡) + 基于本证知识清单的 spaced-repetition 复习提醒；进度接入"我的进度"页。
- 三套已落地（导航「进阶考证」组 + 各自 daily/check 两个 section）：
  - 软考·数据库系统工程师：2027-03-15 → 2027-05-23，10 周/70 天；依据《数据库系统工程师教程（第4版）》+ 上午75选择/下午5案例；知识清单 28 项。
  - 数据开发工程师（ACP→Databricks）：2027-05-15 → 2027-08-07，13 周/91 天；依据阿里云 ACP 模块占比(MaxCompute25%/DataWorks25%/Flink+Hologres30%/ES10%/湖5%/可视化3%) + Databricks 2026-05 七域；知识清单 26 项。
  - CDGP：2027-08-08 → 2028-02-07，26 周/182 天；依据单选+多选+6论述(各≥40%)、六大高频主题；知识清单 27 项。
- 验证：整个 inline script `new Function` 语法 OK；三个 buildGenDaily 调用、三个 nav-item、三个 section、-total 均就位。
- 待用户手动 git commit（index.html 改动，git 嵌套 bug 同前）。

## 软考每日中心 UI 修复（2026-08-16 收尾 #2）
- 用户截图反馈软考每日中心"渲染草率"：打卡日历呈细长浅灰条、日期显示 Day1=2027-03-14（与配置 start 2027-03-15 差一天）、周芯片无当前周高亮。
- 根因：
  1. 热力图容器 `.gen-heat` 用了 `grid-template-columns:repeat(14,1fr)`，但 `.hl-cell` 固定 14×14px，导致每个 grid item 被拉宽、cell 只占左上角，呈现一堆空白长条。
  2. 日期函数 `genTodayISO`/`genAddDays` 用 `toISOString().slice(0,10)`，在 GMT+8 时区把本地 00:00 转成 UTC 前一天 16:00，导致所有日期少一天。
  3. 周芯片没有根据当前选中日在 `genRenderDay` 里更新 `active` 类。
- 修复：
  - CSS：`.gen-heat` 改为与 `.heat-grid` 同款 `grid-template-rows:repeat(7,14px);grid-auto-flow:column;gap:3px;width:max-content`，并共用 hover 效果；去掉三个 `.gen-heat` 的 inline grid 样式。
  - JS：日期函数改为 `new Date().toLocaleDateString('sv')` / `d.toLocaleDateString('sv')`，保证本地时区正确；新增 `genDiffDays(start,iso)` 替换原先基于 UTC 的 idx 计算。
  - JS：`genRenderDay` 里同步更新当前周芯片 `.active` 高亮；热力图 title 改为 `iso wd · Wn`、加 `data-w` 属性。
- 验证：node 本地测试 `genAddDays('2027-03-15',0)` → 2027-03-15 周一（与配置一致）；`new Function` 语法 OK。
- 待用户硬刷新浏览器查看，仍需手动 git commit。

## 侧边栏逻辑重构 + 每日中心合并（2026-08-16 收尾 #3）
- 用户反馈：左侧边栏文档逻辑不合理，且希望每日学习计划合并成一页、只在一页上标注所属考试，尽量少页面。
- 侧边栏重构为 5 个语义分组：🎯 今日（每日学习中心 / 我的进度）、📘 CDGA 主备考（半年规划 / 自查清单 / 自测题）、🏆 进阶考证（三级进阶 / 数据开发证书 / 软考·数据库系统工程师 / 数据开发自查 / CDGP）、🔗 资料资讯（资料清单 / 推文精选 / 关注源）、🏛️ 读博 交大（申请计划 / 推荐信 / 网址）。
- 每日中心"合并成一页"落地方式：保留 4 个 stage section（cdga-daily / ruankao-daily / datadev-daily / cdgp-daily），但侧边栏只留一个入口 `data-page="daily"`；showPage('daily') 按当前日期自动落到对应考试阶段（EXAM_STAGES 配置：CDGA 2026-08-15~2027-02-12 → 软考 2027-03-15~05-23 → 数据开发 2027-05-15~08-07 → CDGP 2027-08-08~2028-02-07）。每个 stage section 顶部有 stage-bar 导航条，标注"当前"所属考试，点击可切换阶段；四个 section 标题均带"· XX阶段"后缀，任务卡片清晰标注所属考试。
- 初始加载由 `showPage('cdga-daily')` 改为 `showPage('daily')`，保证任何日期打开都自动落到正确阶段。
- 验证：node `new Function` 校验整段 inline script 语法 OK=1 BAD=0；4 个 stage section 的 date picker / 热力图 / render 绑定均就位，无重复 id。改动全在 index.html，待用户手动 git commit。

## 进阶考证自查清单合并（2026-08-16 收尾 #4）
- 用户截图反馈：侧边栏「进阶考证」组下仍有 5 个条目（三级进阶 / 数据开发证书 / 软考 / 数据开发自查 / CDGP），质问"有什么好合并"。
- 合并动作：把软考·数据库系统工程师、数据开发工程师、CDGP 数据治理专家三份独立自查清单 section（ruankao-check / datadev-check / cdgp-check）合并为单一页面 `advanced-check`，顶部 tab 切换三证。
- 侧边栏「进阶考证」组从 5 项减为 3 项：🏆 三级进阶总规划、🛠️ 数据开发工程师证书、✅ 进阶考证自查清单。
- 同步更新：`titles`/`GROUP` 移除 3 个旧 id 增加 `advanced-check`；三证 `checkPageId` 改为 `ruankao-check-list`/`datadev-check-list`/`cdgp-check-list`；`genConceptText` 查询目标同步改；`renderProgress` 增加三证自查进度卡片；新增 tab CSS + `initAdvancedCheckTabs`。
- 各证 localStorage 存储键独立不变（bb-ruankao-*/bb-datadev-*/bb-cdgp-*），勾选进度不丢。
- 验证：`<section>` 18 开 18 闭、`<div>` 582 开 582 闭；node `new Function` 语法 OK=1 BAD=0。改动全在 index.html，仍需用户手动 git commit。

## 进阶考证总规划合并（2026-08-16 收尾 #5）
- 用户截图反馈：侧边栏「进阶考证」组仍有「三级进阶总规划」和「数据开发工程师证书」两个独立入口，建议合并。
- 合并动作：把 `cdga-ladder` 与 `data-dev` 两个 section 合并为单一页面 `advanced-plan`，顶部 tab 切换「🏆 三级进阶总览」与「🛠️ 数据开发工程师证书」。原两个页面的 checklist（#ladder-checks / #datadev-checks）及 localStorage 键（bb-ladder-checks / bb-datadev-checks）保持不变，进度不丢。
- 侧边栏「进阶考证」组从 3 项减为 2 项：📈 进阶考证总规划、✅ 进阶考证自查清单。
- 同步更新：`titles`/`GROUP` 移除 `cdga-ladder`/`data-dev`，新增 `advanced-plan`；`renderProgress` 中「三级进阶行动清单」改为 `countChecked('ladder-checks')` 并新增「🛠️ 数据开发行动清单」卡片，均指向 `advanced-plan`；tab 切换函数通用化为 `initTabs(wrapId)`，并新增 `switchAdvancedTab(tabId)` 用于页面内跳转；原 cdga-ladder 中两处指向 `showPage('data-dev')` 的链接改为 `switchAdvancedTab('datadev-panel')`。
- 验证：`<section>` 17 开 17 闭、`<div>` 583 开 583 闭；node `new Function` 语法 OK=1 BAD=0；无 `cdga-ladder`/`data-dev` 残留引用。改动全在 index.html，仍需用户手动 git commit。

## 博士申请计划顶部精简（2026-08-16 收尾 #6）
- 用户截图反馈：phd-plan 页面顶部「🔴 关键修正」「跨学科约束」两个醒目 callout 没必要写出来。
- 精简动作：删除两个 callout；把关键信息（硕士 2025-03 日本取得、已工作约 1.5 年、本科理工科满足学科背景要求）合并进标题下方的 `doc-sub`；同时去掉标题里的「（修正版 v2）」字样。
- 验证：`<section>` 17 开 17 闭、`<div>` 581 开 581 闭；node `new Function` 语法 OK=1 BAD=0。改动全在 index.html，仍需用户手动 git commit。

## 博士申请三页面合并（2026-08-16 收尾 #7）
- 用户截图反馈：侧边栏「读博 交大」组仍有「申请计划 / 推荐信格式与模板 / 申请网址汇总」3 个入口，建议合并。
- 合并动作：把 `phd-plan` / `phd-reco` / `phd-urls` 三个 section 合并为单一页面 `phd`，顶部 tab 切换「📋 申请计划」「✉️ 推荐信」「🌐 网址汇总」。
- 侧边栏「读博 交大」组从 3 项减为 1 项：🏛️ 博士申请。
- 同步更新：`titles`/`GROUP` 移除 3 个旧 id，新增 `phd`；首页 cat-card 的 `data-goto` 由 `phd-plan` 改为 `phd`；tab 通用函数 `initTabs('phd')` 注册新页面切换。
- 验证：`<section>` 15 开 15 闭、`<div>` 581 开 581 闭；node `new Function` 语法 OK=1 BAD=0；无 `phd-plan`/`phd-reco`/`phd-urls` 残留页面引用。

## 精简进阶考证总规划页面（2026-08-16 收尾 #9）
- 用户截图反馈：「进阶考证总规划」页面内容太啰嗦，不够简洁。
- 精简动作：重写 `advanced-plan` 整个 section，从约 26KB 压到约 13KB（-13KB）。
  - 标题下方 `doc-sub` 从长句缩短为一句路线说明。
  - 删除顶部「适合你」大 callout、冗长的「证书有效期/续证」整节、「会改变现在的计划吗」整节、详细的「资料获取清单」整节、datadev-panel 内重复的「软考排期已调整」tip 与长说明。
  - CDGE/CDMP 远期内容折叠进 `details` 摘要。
  - 保留：五级证书卡片、5 年时间轴、CDGP 报考条件与题型表、论述题 6 主题卡片与答题模板、预算条、两个 checklist（ladder-checks / datadev-checks）。
- 验证：`<section>` 15 开 15 闭、`<div>` 525 开 525 闭；node `new Function` 语法 OK=1 BAD=0。

## Git 提交（2026-08-16 收尾 #8）
- 用户要求"直接帮我 git"。诊断：仓库真实 `.git` 在 `D:/01_Projects/.git`（工作树根 `01_Projects`），`learningDesktop/.git` 是被忽略的 stray 目录；AI 实际可正常 commit（纠正旧记忆里"无法本地 commit"的误判）。
- 执行：`cd /d/01_Projects/learningDesktop && git add index.html && git commit -m "refactor(学习桌面): 合并侧边栏与多页面入口，精简导航"` → 提交成功 `5bb3eb0`，仅 `learningDesktop/index.html`（1 file changed, 4403 insertions(+)，首次纳入 01_Projects 仓库跟踪）。`company_coding/dify` 等无关改动未触碰。
- 已同步更新 MEMORY.md 的页面清单与 Git 提交方式说明。

## 学习桌面独立化为 git 仓库并推送 GitHub（2026-08-16 收尾 #10）
- 用户要求把 learningDesktop 当独立仓库处理（git 初始化 + 加 remote）。诊断发现：所谓的 stray `.git` 其实是个**损坏的半截仓库**（pack 的 .pack 文件丢失、refs 结构不全），但 config 里早就配好 `origin = git@github.com:barbarayezi/learningDesktop.git`，且 GitHub 上该仓库 main 已有历史（36760f2）。git 因本地 .git 无效而向上解析到 01_Projects/.git，导致之前误提交到 01_Projects。
- 处理：备份损坏 .git 为 `.git.bak` → `git init` 重建干净仓库 → 补 `remote.origin.fetch` refspec（该环境 remote add 漏写）→ `git fetch origin` 拉到 36760f2 → 用 `git update-ref` 建 origin/main（该环境 ref 自动写入有 bug）→ 把当前工作区快照提交到临时分支 → 基于 36760f2 建本地 main → `git checkout temp -- .` 恢复新版 → `git rm -r 01_考证 02_读博` 显式删除旧 md（保留历史，fast-forward 推送）→ commit `3925820` → `git push -u origin main` 成功。
- 结果：GitHub learningDesktop 仓库 main = 3925820，与本地一致；今日全部重构 + 单一来源（删 11 份 md）+ 新增 proxy_server.py + .gitignore 排除 Find 均已上线。本地 origin/main tracking 因环境 ref 写入 bug 在 `git status` 显示 `[gone]`，不影响实际推送（ls-remote 已验证一致）。
- 待办：上层 01_Projects 仓库仍跟踪 learningDesktop 文件，存在嵌套仓库冲突，建议用户在 01_Projects 用 `git rm --cached -r learningDesktop` 解除跟踪（或加 .gitignore）；此举不影响本次 GitHub 推送。`.git.bak` 为损坏备份，可删。

## 解除 01_Projects 对 learningDesktop 的跟踪（2026-08-16 收尾 #11）
- 用户确认"要"，执行解除嵌套跟踪。诊断：01_Projects 仅把 `learningDesktop/index.html` 当普通文件跟踪（mode 100644，非 submodule，无 .gitmodules、无 gitlink）。
- 执行：`cd /d/01_Projects && git rm --cached -r learningDesktop` → 索引中 learningDesktop 条目清空（`git ls-files learningDesktop` 为空）。
- 在父仓库 `.gitignore` 末尾追加 `learningDesktop/`（第 23 行），`git check-ignore` 命中，父仓库不再把 learningDesktop 显示为未跟踪目录。
- 结果：嵌套冲突彻底解决。learningDesktop 索引中保留一条暂存删除 `D learningDesktop/index.html`（未提交，因 01_Projects 另有大量无关改动如 company_coding/dify 删除，需用户单独在 01_Projects 提交才永久生效）。
- 验证：learningDesktop 自身仓库完好（工作树干净、main=968dcae、与 origin/main 一致）。

## 「我的进度」页按目标聚合重构（2026-08-16 收尾 #12）
- 用户看截图质疑旧设计：12 张同级卡片平铺、按"打卡类型"而非"备考目标"分组、主次不分、顶部统计与卡片重复。
- 重写 `index.html` 的 `my-progress` 页：
  - 顶部 3 个指标卡：当前连续(天)、累计打卡(CDGA 2/182)、总体完成率(全部模块聚合%)。
  - 「当前主攻」卡片：currentStage() 判定，显示阶段名/日期区间/本周主题(Day N · 本周focus)，CTA「进入今日学习 →」→ showPage('daily')。
  - 「备考目标总览」：CDGA 主备考(高亮展开，聚合 每日/自查/自测/视频 4 子项) + 进阶考证(默认折叠，聚合 软考/数据开发/CDGP 各 每日+自查，点击标题展开) + 读博·交大(静态链接 phd)。
- 删除旧 `progCard`/`.prog-banner`/`.prog-grid` 平铺逻辑，新增 CSS：.prog-metrics/.metric-card/.prog-today/.goal-card/.goal-head/.goal-subs/.goal-go 等；用 --accent/--surface/--border 等既有 token，保持扁平风。
- 数据仍从原来源读：DAILY_KEY/QUIZ_STORE/countChecked('cdga-check'|'cdga-res'|'ladder-checks'|'datadev-checks'|'ruankao/datadev/cdgp-check-list') + window.GEN[prefix].store(每日) ，无新增存储键。
- 验证：node `new Function` 校验 2 个 script 块语法 OK=0 错误；静态复刻聚合逻辑确认分组/百分比正确（新用户 0%，CDGA 聚合 0/310、进阶考证 0/413）。
- 未 commit（沿用"index.html 改动手动 commit"约定）。浏览器需硬刷新查看。

## 「半年复习规划」并入每日学习中心（2026-08-16 收尾 #13）
- 用户截图质疑：侧边栏「CDGA 主备考」组下「🗺️ 半年复习规划」单独成页没必要——其"阶段路线图+逐周明细"与每日学习中心高度重叠。
- 合并动作：把 cdga-plan 的「四、半年长线复习路线图（甘特图）+ 4.1 逐周明细（26 周表）」搬到 `cdga-daily` 顶部，包成 `<details class="roadmap">` 可折叠区（默认折叠，summary 提示"点开看 26 周逐周明细"），折叠区末尾加「📖 看完整备考百科 →」按钮（data-goto="cdga-plan"）跳原页。
- 侧边栏「CDGA 主备考」组从 3 项减为 2 项：✅ 知识点自查清单、📝 原创自测练习题（「🗺️ 半年复习规划」入口删除）。
- 首页 cat-card「考证 · CDGA」的 data-goto 由 cdga-plan 改为 daily（直接进每日学习中心）。
- 新增 CSS：.roadmap / summary 折叠箭头 / .rm-h2 / .roadmap-foot（沿用 --accent/--border/--surface token）。
- 保留 section#cdga-plan 完整"备考百科"内容（考试全貌/17知识域/拿分盘/项目映射/避坑/里程碑），仅不再出现在导航；旧书签/按钮仍可访问。
- 验证：node `new Function` 语法 OK=0 错误；details 4/4、section 15/15 配对；data-page="cdga-plan" 残留 = 0（入口已删）。改动全在 index.html，未 commit。

## 「资料资讯」三页合并（2026-08-16 收尾 #14）
- 用户截图质疑：侧边栏「资料资讯」组下「资料获取清单 / 公众号推文精选 / 关注源」3 个入口都是"资料/资讯"聚合页，应合并。
- 合并动作：`cdga-res` 升级为「📚 资料与资讯中心」，内部 tab 切换三个面板：
  - res-list（默认 active）：原资料获取清单全文（官方教材/真题/信息源/活资料/免费备考路径/B站视频日程与 26 周打卡表）
  - res-news：原 cdga-news 全文（推文阅读器：news-bar/news-cats/news-filters/newsList + 两个 callout）
  - res-sources：原 cdga-sources 全文（关注源：src-bar/src-cats/srcGrid）
- 删除原 cdga-news、cdga-sources 两个独立 section；侧边栏「资料资讯」组从 3 项减为 1 项「📚 资料与资讯」。
- 同步：titles/GROUP 移除 cdga-news/cdga-sources、cdga-res 标题改「资料与资讯」；showPage('cdga-res') 触发 renderNews+renderSources；initTabs('cdga-res') 注册；新增 switchResTab(tabId)；关注源卡片「看全部 N 篇 →」与文章标题点击改为 showPage('cdga-res')+switchResTab('res-news')。
- 兼容性：renderNews/renderSources 自带 IIFE 在加载时渲染（只依赖 #newsList/#srcGrid 存在，tab 内 DOM 仍在）；视频打卡 #cdga-res .vchk 选择器不受 tab 层级影响；localStorage 键（bb-video-checks / cdga-news-seen / notes）不变，进度不丢。
- 验证：script 2 块 0 语法错误；section 13/13 配对；tab-content 11 = tab-btn 11；cdga-news/cdga-sources 残留引用 0；关键 id（newsList/srcGrid/res-*）唯一；原资料清单标志内容仍在。改动全在 index.html，未 commit。
- 已 commit+push：`d8d986b`（3 files changed, 287 insertions, 91 deletions），`968dcae..d8d986b main -> main`，工作树干净。本次会话全部改动（我的进度页重构 #12、半年规划并入每日中心 #13、资料资讯合并 #14 + 记忆更新）现已在 GitHub main 上。

## 资料资讯中心：站内 3 tab → 2 tab（#15）
- 用户追问：站内 cdga-res 现在还是 3 个 tab（资料获取清单 / 公众号推文精选 / 关注源），真的有必要分开吗？判断：推文与关注源是「同一件事两面」（谁发 vs 发了什么），应合并；资料清单是静态参考，性质不同应保留。
- 改动：tab 从 3 个减为 2 个——保留 `res-list`（资料获取清单），将 `res-news`+`res-sources` 合并为单个 `res-info`：上方「关注源」卡片网格、中间分隔条、下方「推文精选」列表（同一页内）。
- 联动交互：点关注源卡片即按该号账号过滤推文（`newsAccounts` 新过滤维度，match `a.account ∈ s.accounts`），卡片高亮 `.active-src`，推文区出现「当前来源：XXX ✕ 看全部」清除 chip，平滑滚动到推文区；再点同一卡片取消；「看全部 N 篇 →」改为按 cat 过滤、不再切 tab；`.src-item` 点击也落到该号过滤。
- 验证：node 抽内联脚本 `new Function` 语法校验 2 块 0 错；无 `res-news`/`res-sources`/`switchResTab('res-news')` 残留引用（`switchResTab` 定义保留但已无调用）。
- 已 commit+push：`13bd07e`，1 file +75/-50，main 干净。
## B站视频打卡：从资料清单迁出 → 每日中心（#16）
- 用户判定：打卡就该在「打卡的地方」，资料清单只做纯参考；打卡区要能切多时间维度（今日/本周/全量），否则不知道看哪边。
- 动作：把 res-list 里那段带 .vchk 的「视频学习周历 W1–W26 打卡表」整块移除（脚本按锚点 删除），资料清单改为一句指向每日中心的说明；打卡全部迁入 cdga-daily（CDGA 阶段页）内新建 #videoChecks 容器。
- 新看板三视角：全 26 周（26 行打卡表，数据集中为 JS 数组 VIDEO_WEEKS 单源渲染）、本周（renderVideoWeek：当前周任务 + 标记本周完成按钮）、今日（renderVideoToday：按周几显示工作日/周末任务）。维度切换 .vb-dims .dim-btn。
- 联动：localStorage 键 bb-video-checks 不变；countChecked(cdga-res)→countChecked(videoChecks)；我的进度 ▶️ B站视频 page 改为 cdga-daily；视频 IIFE 末尾调 updateProgress() 刷新汇总。
- 坑：首版漏给看板包 id=videoChecks 容器，导致 countChecked 与打卡绑定全部失效，已补 <div class="res-block" id="videoChecks"> 包裹修复。
- 验证：内联脚本 2 块 0 语法错；cdga-res .vchk 残留 0；#videoChecks 开合各 1；#videoChecks .vchk 引用 4 处。
- 已 commit+push：4242558（2 files, +4518/-4439），main 干净。
## 博士申请页 3 tab 合并为 1（#17）
- 用户判定：申请计划/推荐信/网址汇总三个 tab 内容上就是一份博士申请手册的三个章节，没必要分开切 tab。
- 动作：删除 tab-nav（3 个 button），将 phd-reco-panel 和 phd-urls-panel 两个 tab-content 块合并进 phd-plan-planels，中间加分隔线标题（✉️ 推荐信 / 🌐 网址汇总）自然分段。
- 验证：内联脚本 2 块 0 错；残留旧 id 引用 0；JS 无 switchPhdTab 调用。
- 已 commit+push：ca0ef4f（2 files, +10/-13），main 干净。
## 博士申请页内容重构精简（#18）
- 用户要求不光合并 tab，还要内容结构重构和精简。
- 诊断出 6 类问题：①「剩余积累窗口」与时间线+行动清单重复 ②「五·B推荐信策略」与下方完整推荐信章重叠 ③「CDGA协同」3条可合并行动清单 ④材料清单第6项推荐信下方有整章展开 ⑤推荐信「硬性要求」表与资格表重复 ⑥编号乱（五→五·B→六→八跳七；推荐信/网址各自从一编起）
- 重构为 7 个干净章节：一时间线 → 二我够不够格(资格) → 三去哪个院系 → 四要准备什么(材料) → 五推荐信⭐(合并原五·B+完整章，5.1-5.7子节) → 六关键网址(压成1张用途分类表) → 七现在该做什么(合并原行动清单+窗口+CDGA协同，加🔴最优先列)
- 删除：剩余积累窗口、CDGA协同独立节、三套嵌套编号、6个散装h2网址小节
- 结果：净减 43 行(-122/+79)，信息零丢失
- 已 commit+push：cf4295d（2 files, +79/-122），main 干净。
## 博士申请准备路径细化（#19）
- 用户指出第七章「现在该做什么」只有3行粗粒度阶段表，没有细化到具体准备计划。要求结合时间线和申请材料，拆成可执行的具体动作。
- 重写第七章为「七、准备路径」：38项可勾选checkbox（localStorage持久化bb-phd-path），分三阶段+材料总检表。
  - 🅰️起步期（现在~2026-12）：9项——续旧邮件(A01)、推荐人备用池(A02)、CSCSE认证(A03)、境内证件(A04)、硕士成绩单/论文(A05)、铺垫领导(A06)、确认岗位方向(A07)、整理项目素材(A08)、CDGA冲刺(A09)
  - 🅱️筑基期（2027全年14项）：Q1拿CDGA+联系导师+定方向(3) → Q2 CDGP+主导项目+写初稿(3) → Q3 CDGP+盯简章+联系交大导师+确定第2推荐人(4) ⭐关键季 → Q4研究计划定稿+投稿+推荐信草稿+第三次联系导师(4)
  - 🅲冲刺期（2028 H1 15项）：1-2月材料收口(5) → 3月工龄达标+单位证明(3) → 4-5月网报(3) → 6-8月复试录取(4)
  - 材料总检清单：10项材料逐行标注状态/办理渠道
  - 进度统计：总完成数 + 分阶段计数（A/B/C各独立）
- 新增CSS：.phd-phase/.phd-task-group/.phd-tasks（勾选后灰化+删除线）
- 新增JS：PHD_KEY + initPhdPath IIFE + updatePhdProgress()
- 验证：内联脚本2块0错；4阶段标题/12任务组/38checkbox/材料总检表全部就位
- 已commit+push：f7049c7（2 files, +181/-11），main干净。
