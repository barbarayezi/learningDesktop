# 2026-08-19

## 跨设备访问/同步问题根因定位与公网部署（重大进展）
- 用户投诉三件事：① 别的电脑看不到 192.168.103.37:8080 的内容；② 数据没同步；③ 网站上改的东西全没 git。
- 实测排查结论：
  - **git 正常**：代码改动（含 a55932b 清理推文）均已 push GitHub。
  - **云端正常、数据已在云上**：用 proxy_server.py 写死的 service_role 密钥直连 CloudBase 网关 `/v1/rdb/rest/user_data`，返回 10 条真实数据（cdga-daily-checks / cdga-quiz-progress-v1 / bb-phd-check 等），最近更新 2026-08-18 19:51。证明云同步基础设施 OK、用户数据已成功上云过。
  - **根因 = 局域网耦合**：192.168.103.37 是内网 IP，只能同 WiFi 访问；且云同步必须经由家里 Mac 的局域网代理（API_PROXY=/api）才能碰云。任何一环断（换网络/IP 变/服务器挂/没 git pull 65e7d40）就全断。
  - **网关拦截浏览器跨域**：带 `Origin: https://example.com` 直连网关返回 403 Forbidden（不带 Origin 的同源/服务端请求 200）。故浏览器必须走"剥 Origin + 注入密钥"的代理，纯直连不可行（除非控制台白名单来源）。
  - **认知纠正**：网页内打卡/笔记是数据（localStorage/CloudBase），本就不进 git；git 只管代码文件。
- 决策：用户选「公网部署 + 改同步」（绕开家里 Mac）。
- 动作 1：index.html 增加 `SYNC_PROXY_URL`（可配置同步代理；留空=同源 /api 局域网模式，填公网代理地址=任意网络同步）。commit 2aab93e，已 push。
- 动作 2：**CloudStudio 公网部署成功**，分享链接 `https://081015d20afb4e698b05ef67d7b17c14.app.workbuddy.link`（verified HTTP 200）。内容已任意设备可访问。
- 待办（同步后端）：CloudStudio 仅支持纯静态，跑不了代理。数据同步仍需一个**公网代理**（剥 Origin+注入密钥）。候选宿主：① CloudBase 云函数（数据已在该项目，无需新账号）；② Cloudflare Worker（免费）；③ 控制台白名单来源+匿名密钥（无后端，但免费版 CORS 支持存疑）。**下一步需用户选定宿主并授权/提供账号**，我再把 SYNC_PROXY_URL 填上重部署即可全通。
- 安全：service_role 密钥绝不能进客户端 JS；代理必须服务端持有。匿名/公开密钥才可放前端。
