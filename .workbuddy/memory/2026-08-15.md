# 2026-08-15

## 考博学习计划 · 海外文章降级处理
- 用户质疑：为何收了国外申博经验？对交大(国内申请-考核制)申博帮助有限。判断正确——国内外的**申请系统**差异大，但**推荐信/套磁手艺**国内外通用。
- 处置(用户选「降级到拓展参考」)：把 8 篇纯国外语境考博文(英国/香港博士招生、留服·境外学历认证等)打 `region:'overseas'`，移出每日学习核心流，归入资讯页「🌏 拓展·海外」分类。
  - 8 篇：`a3c86fe9874`(国外博士推荐信)、`abe0e446a85`(英国博士流程)、`a85aeb638ad`(香港中文大学)、`ac20683385f`/`a8625a43379`/`a7717207aaa`/`a94c7aff03b`/`ac075e913ef`(留服/境外/菲律宾学历认证)。
  - **保留**在核心流的 5 篇推荐信/套磁手艺文：`a4266ce289d` `a6f04418953` `ad2a56c5837` `ab4b5e8cbe2` `a92ada05b22`(交大也要套磁+2封专家推荐信)。
- 改动文件：
  - `资料/articles.js`：全部条目自愈式重算 `region`；ARTICLES_UPDATED=2026-08-15 00:12。
  - `资料/harvest_articles.py`：新增 `OVERSEAS_RE` + `region_for()`(仅考博类、强国外-system信号才判海外)，`make_entry` 加 `region` 字段，导出前对所有条目重算→下次抓取不丢标记。
  - `index.html`：`buildDailyArticles()` 排除 `region==='overseas'`；`renderNews()` 的 cat 筛选支持 `overseas`；资讯页加「🌏 拓展·海外」按钮 + 说明 callout。
  - `资料/README_公众号监控.md`：补海外降级设计说明与 stats。
- 验证：内联脚本语法 OK；每日流 118→110(无海外)；考博筛选 57→49；海外分类正好 8 篇。
- **长期偏好**(见 MEMORY.md)：学习计划聚焦交大(国内申请-考核制)，国外经验默认降级到拓展，不要混进每日核心流。

## GitHub 推送 + CDGA 三页去重 + 每日中心主动投递
**GitHub**: 已 `git init` + 首次推送 40 个对象到 `git@github.com:barbarayezi/learningDesktop.git` (main 分支)。
**问题**: 用户截图发现 CDGA 侧三页(半年复习规划 / 26 周计划 / 知识点自查)有重叠，且担心"放在那里不看 = 等于忘了"。诊断：信息"存在 ≠ 被吸收"。captured/surfaced/reinforced 三态缺失。
**去重方案**:
- 删独立 `#cdga-weekly` 整页(用 sed 精确删 1049-1114 行)
- `cdga-plan` 删重复的「阶段与周次总览」表
- `cdga-plan` 新增 4.1/4.2/4.3 节：26 周逐周明细表 / 课程观看策略 / 每日节奏模板
- nav / titles / GROUP 同步清理
**主动投递方案**(每日学习中心)：
- 🔁 复习提醒：勾掉清单项时记 mastered_at，按 3/7/14/30/60/90 天循环再次提醒(±1 天窗口)
- 🎯 本周主题 + 今日 3 个关键概念：根据 WEEK_DOMAINS 映射到 cdga-check 索引，每天从当前周对应域的未勾概念里抽 3 条
- ✓ 仍掌握 → 重置 mastered_at 进入下一周期；× 忘了 → 取消勾选回本周主题
**技术新增**:
- `bb-cdga-check-dates` localStorage: `{i: ISO-date}`
- `WEEK_DOMAINS / DOMAIN_IDX / WEEK_META` 数据表 (W1-W26 → 知识域 ID → checkbox 索引)
- 概念文本从 `#cdga-check` DOM 单一来源抽取 (escH 转义)
- `setCheckState(i, checked)` 统一改清单状态 + 同步 mastered_at + 同步主页 checkbox
- 新 CSS: `.dc-week / .dc-concepts / .dc-concept / .dc-review / .dc-review-item / .dc-cycle-tag.c3..c30`
**commit**: `b64da2d` feat: 去重 cdga-weekly + 每日中心主动投递(spaced repetition) (+210/-83)
**验证**: 内联脚本语法 OK；W3 推 [0,1,2](治理3概念)、W4 推 [5,6,7](质量3概念)、今天 2026-08-29 复习队列准时弹出 4 天前学的内容。

## 资料阅读器 · Markdown 渲染
- **问题**：用户截屏显示 `.md` 文件当 `iframe.src` 加载，浏览器把 `**`/`##`/`>`/`-` 等语法当纯文本显示，可读性极差。
- **修复**：自实现轻量 Markdown 渲染器(约 50 行，无外部依赖)。检测 `.md` 扩展名 → `fetch()` 文本 → 渲染为 HTML → 注入 `#mdHost` 容器；其他格式(`.html`/`.pdf`)继续走 iframe 兼容；md fetch 失败自动回退到 iframe。
- **支持的语法**：#-###### 标题 / 水平线 `---` / `**粗体**` / `*斜体*` / 行内代码 / ` ``` fenced ``` ` 代码块 / `>` 块引用 / `-` 无序列表 / `1.` 有序列表 / `[text](url)` 链接 / GFM 表格。
- **样式**：`.md-host` 全套(标题左侧 accent 条、加粗高亮、行内代码卡片、blockquote 蓝底、表格 border、清单左对齐)，跟随日/夜间主题切换；加 loading 旋转动画与 `.md-err` 错误样式。
- **新增函数**：`mdEsc(s)`、`mdInline(s)`、`mdRender(md)` + `openReader()` 内 `ext==='md'` 分支。
- **commit**: `92e0804` feat(md): 资料阅读器支持 Markdown 渲染(+137/-2)
- **验证**：用真实笔记前 45 行跑通——统计 1×H1 / 3×H2 / 1×H3 / 2×blockquote / 1×table / 3×ul / 3×hr / 23×strong / 6×code。

### 资料阅读器 · Markdown file:// 渲染失败修复（user 反例）
- **用户截图**：点 11域备考笔记.md 仍显示 `**#**`、`> `、`~~` 原始文本。
- **根因**：`openReader` 用 `fetch(r.file)` 拿 md 内容，但 **Chrome 默认禁止 file:// → file:// fetch**（file origin 视为 null，CORS 直接拒）。catch 兜底切到 iframe，iframe 把 `.md` 当纯文本显示——这就是用户看到的画面。
- **`mdRender` 本身没问题**：Node vm 里跑 `mdRender('DMBOK2R_11...md')` 输出 8549 字标准 HTML（`<h1>`/`<table>`/`<blockquote>`/`<hr>`/`<strong>`/`<code>` 全有）。
- **修复（绕开 file fetch 限制）**：
  - 在 `</div>` 后内嵌 `<template id="mdSrc-notes">…</template>`（6.1KB，可接受）装 11域备考笔记.md 全文。
  - `openReader` 的 `.md` 分支改为：**优先** `document.getElementById('mdSrc-'+k).content.textContent` → `mdRender`；**找不到内嵌才**走 fetch（保留 http/https 兼容）；fetch 失败仍兜底 iframe（最差情况回到原始文本）。`mdRender` 抛错显示 `.md-err`，不再静默退化。
  - 文件增长 183817 → 190508 字节（+6.7KB），单文件可接受。
- **commit**: `c3d92c0` fix(md): 内嵌 `<template>` 绕开 file:// fetch 限制 (+179)
- **校验**：内联脚本语法 OK；template 长度 = 源 .md 长度；已 push 到 origin main。
- **下次新增 .md 资源时**：在 RES 加 `{key,name,file}`，再在 body 内嵌 `<template id="mdSrc-{key}">…</template>`。

## 每日学习中心 · 视频筛选（用户明确反对"自己搜"）
- **问题**：用户截屏发现每日中心的视频链接是 `U.search`（B站搜索链接），吐槽"让我自己搜索选择视频吗？我还指望你帮我筛选呢"。
- **修复（用户要的是"帮我筛好可直接点"）**：
  - `U` 新增 **17 章 CDGA 备考视频** `c1`–`c17`（北大青鸟总部 2025 修订版 17 章系列，已逐个 WebSearch/WebFetch 验证 BV id）+ `whole`(6h 速成 BV1tFcWzeEra) + `whole2` + `exp`(一次过经验 BV1vJ4m1j79C)。
  - `WEEKS` W1–W24 每周 `videos` 改为指向**具体章节 BV 视频**（如 W3 = Ch3 数据治理 BV1LWS7BeETP），不再用搜索链接。
  - **修复 bug**：W25/W26 误用 `U.search`（未定义 → 渲染 undefined）。改指向独立 `const search='https://search.bilibili.com/all?keyword=CDGA+北大青鸟'`，仅作冲刺周兜底。
  - Ch16 北大青鸟没有，改用 `mydama` 的 BV1Rdbpz5EaX；其余 16 章 + 2 个总论/经验视频均来自 北大青鸟总部。
- **commit**: `1e8622f` fix(daily): 用具体章节视频替换"自己搜"链接，修复 W25/W26 U.search 引用 (+62/-39)
- **验证**：内联脚本语法 0 错；26 周全部解析为有效 http URL；W25/W26 正确回退 search 兜底。已 push 到 `git@github.com:barbarayezi/learningDesktop.git` (8325d4f..1e8622f)。

---

## 2026-08-15 第三次迭代：阅读原文链接修复 + md bug 重现

### 1) 「阅读原文 ↗」改造成「🔍 微信内搜」+「📋 复制出处」
- **用户截屏**：每日中心的「📰 今日推文」底部点「阅读原文」→ 外链显示「链接已过期」。
- **根因**：每日中心 + 公众号推文精选两处的 `<a class="news-read" href="${art.url}">` 指向 `weixin.sogou.com/link?url=...` 这种**搜狗微信搜索中转 URL**，几天到几周后公众号文章下线或被举报就 404。118 篇里有 82 篇没内嵌全文（仅有点要点），所以「阅读原文」是唯一出路——但它失效率高得不能接受。
- **改造方案（两处 + 全局监听）**：
  - `<a class="news-read" 阅读原文 ↗</a>` → 两个 `<button>`：
    - `🔍 微信内搜`：`data-title="${art.title}"`，点击 `window.open('https://weixin.sogou.com/weixin?type=2&ie=utf8&query=' + encodeURIComponent(title))`。搜狗搜索 URL 本身不会过期，结果是真实公众号文章。
    - `📋 复制出处`：`data-url="${art.url}"`，写 `navigator.clipboard.writeText`，弹底部 toast。
  - 加 `showToast()`（插入 `<div id="toast">`）+ 全局 `document.addEventListener('click', …)` 拦截 `.news-search` / `.news-copy`。
  - **file:// 兼容性**：`navigator.clipboard` 在 `file://` 下被禁（不满足 secure context），加 `document.execCommand('copy')` 的 textarea fallback。
  - CSS：`.news-read` 加 `border:none; cursor:pointer; display:inline-flex` 让 `<button>` 长得跟原 `<a>` 一致；`.news-foot` 加 `flex-wrap` 防窄屏溢出。
- **commit**: `c65176c` fix(news): 废弃易失效的微信中转链接，改用微信内搜 + 复制出处 (+48/-4)
- **验证**：内联脚本 0 错误；旧 `<a class="news-read">` 计数 0；2 处都换成 2 个 button；showToast / addEventListener 各 1 处；URL encode 验证 OK。

### 2) Patch 流程踩坑（避免再犯）
- **坑 1**：用 `node` 脚本 + `s.replace()` 做字符串 patch 时，**在 template literal 里写 JS 代码一定要重新拷一遍**。这次我在 `/tmp/patch_wechat_link.js` 的 template literal 里手抖把 `const fail=()=>{` 写成 `const fail=()>{`（少了 `=`），结果 Node `--check` 报「Unexpected token ')'」指向那一行——而我反复 bisect 还以为是更早的代码出问题，浪费了好几轮。
- **教训**：
  - 写完 patch script **立刻 `node --check <extracted_script>`**，不要相信肉眼检查。
  - Node 报错位置可能会指错行（因为语法错误暴露点 ≠ 出错点），但只要 `node --check` 0 error 就是 OK 的；不要试图从 bisect 反推 bug 行。
  - diff 工具要带 `--from-file/--to-file` 而不是只 diff 行——这次是字节级差异，肉眼一样但 hex 不同。
- **坑 2**：Chrome `file://` 下不能用 `fetch` 拿到同目录的 `.md`。**当前对策（commit c3d92c0）**：把要展示的 .md 内容**内嵌**为 `<template id="mdSrc-{key}">…</template>`，openReader 读 `tpl.content.textContent` 绕过 fetch。后续若加新 .md 资源，必须同步加 template，否则又会被截屏揭穿「没渲染」。


---

## 2026-08-15 第四次迭代：今日目标「全量刷题 92题」按钮降级

### 用户截屏 → 问题定位
- **场景**：W1 周六（今天 2026-08-15，stage=`准备`），「自测/做题」区出现 `🎯 全量刷题 <span>92题</span>` 主按钮（红色高亮）。
- **用户原话**：「这是啥意思，让我一天之内把所有的题目全都做完吗？」——典型**视觉欺骗**：按钮样式 + 巨大数字 = 让人误以为「今天是必答任务」。
- **根因**：`renderDay` 里的 `quizBtns` 用 `dom.map(k=>...)` 一刀切，**不区分 stage**，导致 W1（准备阶段）和 W24（刷题阶段）渲染相同的高亮 practice 按钮。

### 修复（commit 31c7ae4）
1. **按 stage 区分渲染**
   - `earlyStage = !!{准备:1, 通读:1, 高分精学:1, 中分精学:1, 低分精学:1}[d.stage]`
   - earlyStage + `domains:['all']` → **只渲染 ghost 按钮**（白底蓝字）：「📖 看全部题（建立题感）」
   - 刷题/冲刺 + `domains:['all']` → 渲染 practice 主按钮 + ghost，**按钮内加进度徽章 `[已答/92 题]`**
   - 单域模式 → 同样加进度徽章 `[已答/总数]`
2. **新增 .dc-tip 蓝色提示卡**
   - 早期阶段文案：「📘 当前阶段无需强行作答。点击看全部题过一遍，建立题感即可。真正练习从 W21 开始。」
   - 后期阶段文案：「🎯 冲刺/全量阶段，按进度作答，不必一天全答完；进度自动保存。」
3. **CSS 新增**：
   - `.dc-prog` —— 按钮内嵌的进度徽章（深色 pill，小字号）
   - `.dc-tip` —— 浅底蓝色边线提示卡

### 进度数据源
- 位置：`practice.answers`（localStorage key `cdga-quiz-progress-v1`）
- 统计方式：`Object.keys(answers).filter(k=>answers[k]).length`
  - 全部题：直接统计
  - 单域：`QUIZ.map((q,i)=>({q,i})).filter(({q})=>q.d===filter).filter(({i})=>practice.answers[i]).length`

### 验证（端到端模拟 8 个场景）
| Week | stage | domains | 渲染结果 |
|---|---|---|---|
| W1 | 准备 | ['all'] | 仅 ghost「看全部题(建立题感)」+ 提示 ✅ |
| W2 | 准备 | ['十一、概述'] | 「🎯 刷题 概述 [1/3]」+ study + 提示 ✅ |
| W14 | 高分精学 | 6 域 | 每个域独立 [已答/总数] ✅ |
| W21 | 刷题 | 6 域 | 同上但用冲刺提示 ✅ |
| W24 | 刷题 | ['all'] | 「🎯 全量刷题 [5/92]」+ study + 冲刺提示 ✅ |
| W25/26 | 冲刺 | ['all'] | 同上 ✅ |

### 反思
- **UX 警示**：高亮主按钮 + 大数字 + 没有「X/Y 进度」 = 误读为「今天必须做完」。任何「全量/全部」按钮必须自带进度可视化。
- **数据-UI 失配**：同一 `domains:['all']` 在 W1/W24/W26 三个不同语义阶段使用，但 UI 一视同仁。**修复模式**：UI 应该按语义维度分流（这里是 stage），不要图省事。
- **踩坑**：第一次 patch 时把 `earlyStage = !{...}[stage]` 写成 `!`，导致 W1 反而显示 practice 按钮——方向反了。**自检纪律**：写完涉及分支判断的代码，**必须**至少走一遍 3 种 stage 的真实数据验证，不能只看 syntax check。

---

## 2026-08-15 第五次迭代：每日中心「学什么/学多久」标签系统

### 用户截屏的核心痛点
- "你直接给我这么一大堆文章，我也不知道该如何学习"（缺治疗标签）
- "就粗略看一遍还是有哪些需要精读、哪些需要背诵、哪些需要在实际运用中融会贯通？这些都没有"（缺分层）
- "也没有生成这些文章的笔记的地方"（缺笔记位）
- "你觉得让我一天看这么许多文章合适吗？"（缺用量预警）
- "你现在给我安排每天看的视频也没有连贯性"（缺系列分组）

### 第五次修复（commit b7841da）—— 只做地基（标签+用量+估时）
- **治疗体系**（4 类）：
  - 🔵 `浏览` / 🟢 `精读` / 🟥 `背诵` / 🟣 `实操`，每类独立 CSS 配色
  - CSS: `.dc-tag-treat.t-{浏览|精读|背诵|实操}`
- **优先级体系**（3 级）：
  - 🔴 `P1` 必做 / 🟡 `P2` 本周内 / ⚪ `P3` 选学
  - CSS: `.dc-tag-prio.p-P1/P2/P3`
- **每项材料元数据**：治疗 + 分钟数 + 优先级尾随显示
  - 视频: `[精读·18min·P1]` 三 chip 横排
  - PDF chip: 同样三 chip
  - 今日推文: 文章头部显示一行小 chip
- **每日用量汇总卡**（顶部）：
  - 共 N 项
  - 总 XhYm（>3h 标红 + 🔥 较重，>2h 标黄 + ⚠️ 提示）
  - P1/P2/P3 各自数量
  - 自动读 `d.videos + RES + art` 聚合计算
- **P2+P3 折叠**：默认折叠到 `🔽 N 项本周内/选学（可点开）`，点击展开
  - label 智能：纯 P2 "本周内可学"，纯 P3 "选学（可点开）"，混合 "本周内/选学"

### 数据侧改动
- `RES` 6 个 PDF 全标 (notes 精读60min P1 / damaPdf 背诵5min P1 / sohu 精读45min P2 / oval/sogeti 浏览10min P2 / dgiPdf 浏览8min P3)
- `WEEKS[0..6].videos` 手工标注：W1=Ch1 精读18 P1 / 6h速成 浏览60 P3 / cv11 精读30 P1 / 经验 浏览12 P3，W2-W7 类似分层
- `WEEKS[7..25].videos` 用 `/tmp/tag_weeks.js` 批量升级，启发式：
  - 标题 `Ch\d+` → 精读20 P1
  - `#\d+ 数语/尚硅谷/哈喽鹏程` → 精读90 P1
  - 速成/经验 → 浏览 P3
  - 其他兜底 → 精读20 P1
- `资料/articles.js` 前 14 篇（DAPOOL 前 2 周）加 t/m/p

### 关键 bug 修复
- **bug 1**: `summarizePlan` 用 `_m(it.m)` 但 `videoMeta` 返回 `minutes` 不是 `m`，导致视频被算成默认 15min，总量少算了 60min（6h 速成那条）
  - 修复：`summaryItems` 构造时统一 normalize 成 `{t, m, p}`
- **bug 2**: `videoMeta` 返回 `{treatment, minutes, prio}`，但 fold/summary 用 `_p(it.p)` 会找不到（默认值 P2），所有视频被折叠
  - 修复：`prio` → `p` 字段统一
- **bug 3**: `tag_articles.js` 的"already tagged"正则 `[\s\S]*?"t"\s*:\s*"` 跨文章边界（lazy 但没限定 `}`），误判 4 篇已加 → 实际没加
  - 修复：`tag_articles2.js` 限定同一对象内（`{...}\s*}`）

### 验证（3 个典型 day 渲染）
| Day | Stage | 总数 | P1 | P2 | P3 | 折叠摘要 |
|---|---|---|---|---|---|---|
| W1 day 1 (今天) | 准备 | 11 | 4 | 3 | 4 | 视频"2 项选学" / PDF"4 项本周内/选学" |
| W2 day 1 | 准备 | 11 | 4 | 4 | 3 | 视频"2 项本周内/选学" / PDF"4 项本周内/选学" |
| W21 day 1 (刷题) | 刷题 | 10 | 5 | 4 | 1 | 视频全 P1 可见 / PDF 折叠 |

### 下一次承诺（用户已确认排下轮）
1. **视频系列分组**：把每日视频从"按章节点"改"按系列 + 今日该看第几条 + 看完打勾"
2. **笔记功能**：每篇文章可写一段本地笔记 + 顶部"今日手记"汇总

### 自检纪律重申
- 写涉及新 schema（t/m/p）的 patch，**必须**实际跑渲染（vm.runInContext + DOM mock）确认数据流通；只看 `node --check` 语法 0 错误是不够的——上次 quiz 按钮的 `!` 反向 bug、这次的 `prio` vs `p` 字段名不统一 bug，都是 schema 字段名错位的典型。
- 字段名一致性检查：用 grep 在整个 codebase 找一个字段的所有出现位置，确保 setter/getter 同名。
- 写批处理 regex 一定要 lazy + 边界检查（`\}\s*}` 之类），避免跨对象匹配。
