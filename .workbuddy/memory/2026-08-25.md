# 2026-08-25

- 修复 Ch12 元数据管理 B 站视频失效链接：BV1p5mEBkESv（UP 主设为仅自见）→ BV14t4y1Z7Hg（老姜在渝，12:17）。同步更新 VIDEO_DUR 时长为 737 秒。改动已 commit `6c22c21` 并 push 到 GitHub main。

- 错题本浏览/刷题双模式（Leitner 间隔）：commit `410ed55`，已 push。当前公网仍返回旧版，因 GitHub Actions Secrets 缺 `TCB_SECRET_ID`/`TCB_SECRET_KEY`。

- 普通刷题修复旧答案残留：每日学习中心/题库汇总卡进入「刷题」时，清空 `practice.answers` 并换 `todaySeed`，保证每轮都是全新可答。`openQuiz(..., 'study')` 不清空。已用 puppeteer 验证。

- 错题本刷题新增「🔁 即时重刷」开关（选题页，默认开）：本轮答错的题自动排到队尾，等其他题刷完后再来一遍，直到连续答对才离开本轮。实现改用队列模型（`wbSession.queue` + `seq` + `requeueCount`），提交判分后点「下一题」时出队，答错且开启即时重刷则队尾追加（每个题最多重刷 3 次封顶防死循环）。HUD 显示「第X题·队列剩N·正确率」，重刷题标注「本轮第N次刷这题」。puppeteer 双测通过：① 全错→重刷→全对；② 始终错→队列回涨、封顶后结束、无死循环。

## 公网部署调试（下午，终态 success）
- 用户要求把公网部署「调试到成功为止」。过程：
  1. 旧 `TencentCloudBase/cloudbase-action@v1`（node12）在 GitHub node24 runner 上登录失败「无有效身份信息」→ run 22/23 失败。
  2. 改用官方 `tcb` CLI：`tcb login --apiKeyId/--apiKey` + `tcb hosting deploy`。Secrets（TCB_SECRET_ID/KEY）经 Pre-check 确认已配好（之前误以为没配是错的）。
  3. 关键坑：自作主张把公网域名后缀 `-1469471831` 当成 envId 一部分 → `DescribeStaticStore` 报 `Code: InvalidParameter`。
  4. 用 `tcb env list`（在 CI 里通过 annotation 暴露输出）确认**真实 envId = `test-d5gf0o9ky7d34beaf`（无后缀）**，环境状态 Normal、region ap-shanghai，密钥权限正常。
  5. 修正 envId + 环境参数改用 `--env-id`（tcb V3 全局选项，`-e` 不被 hosting 子命令识别）→ **run 29 (commit `01dc60b`) 首次 success**。
- 公网验证：`curl 公网 | grep` 出现 2 处 `BV14t4y1Z7Hg`、0 处旧 `BV1p5mEBkESv` → 部署成功且站点已是最新版。
- **重要记忆修正**：envId 真实值无后缀（域名后缀 ≠ envId），已同步更新 MEMORY.md「公网部署」整段，避免再犯。deploy.yml 现用官方 tcb CLI，不再用 cloudbase-action@v1。
- 调试技巧（无日志权限下排查 Actions）：在 step 里 `echo "::error::$line"` 主动发 annotation，再读 `check-runs/<id>/annotations` API（logs API 需 auth 读不到）。GitHub 未认证 API 有缓存，用 `actions/runs?per_page=2&sort=created&direction=desc` 绕过。

## 错题本「今日复习完成」视觉反馈（晚）
- 用户反馈：每日中心「错题本复习」卡片刷完后没有视觉变化，难以感知任务完成。
- 实现：
  - 新增 `WB_DAILY_DONE_KEY='cdga-wb-daily-done-v1'`，按日期记录「今日错题复习已完成」。
  - `renderWrongbookSummary()` 中，若本轮从「今日到期」入口进入且已完成，调用 `saveWbDailyDone(true)`。
  - `wrongBookDailyBtn()` 渲染时：已完成 → 绿色按钮 `✅ 错题本复习 今日已复习`；未完成 → 原「今日 N 道到期」。
  - `showPage()` 切换到任意每日学习中心阶段时，重新 `renderDay(dayDate.value || TODAY_ISO)`，保证从错题本返回后按钮状态刷新。
  - 新的一天自动重置（`loadWbDailyDone()` 检查日期是否匹配）。
- CSS：新增 `.dc-btn.success { background:var(--green); border-color:var(--green); color:#fff }`。
- 验证：puppeteer 跑通「初始显示今日 1 道到期 → 完成今日到期刷题 → localStorage 标记 done → 返回每日中心按钮变绿色 ✅ 今日已复习」。
