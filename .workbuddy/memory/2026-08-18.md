# 2026-08-18

## 跨设备云同步彻底修复（commit 65e7d40）
- 用户反馈"数据没在所有设备同步"。根因：旧 CloudBase 密钥过期一年 + 代理从未注入密钥 → cloudReady 永 false → setItem 拦截器直接 return，数据不上云。
- 用户提供新密钥（解码确认 `role=service_role`、永不过期、绕过 RLS）。
- 改动：`proxy_server.py` 换成新 key 并强制注入 `apikey`/`Authorization: Bearer`；`index.html` 的 `initCloud` 容错（匿名登录失败不阻断）。
- 验证：本地测试代理对真实网关跑通 GET/upsert/select/delete 全 200/201/204；云端 `user_data` 已有用户真实数据（cdga-daily-checks 含 08-15/16/17），无数据丢失。
- 注意：运行中的 192.168.103.37:8080 服务器须 `git pull` + 重启 `proxy_server.py` 才生效。
- 安全：service_role 密钥硬编码进 git 跟踪文件，public 仓库应改环境变量。
- 排错插曲：误用错误 grep 子串以为 HEAD 没提交密钥，实际 65e7d40 已含正确改动，虚惊一场。

## 重启 8080 代理服务器（本次请求）
- 用户要求我代劳重启。确认 8080 即 learningDesktop 代理（`SimpleHTTP/0.6 Python/3.12.10`）。
- 结束旧进程 PID 2876，用 managed Python 3.13.12 重新启动 `proxy_server.py`（后台任务 0DHGhd）。
- 验证：curl 首页 200；`/api/v1/rdb/rest/user_data` 返回 200 及云端数据；puppeteer 确认浏览器端 `cloudReady=true`、日志打印「✅ 云端已连接，同步了 10 条数据」。
- 跨设备云同步现已生效。

## 清理考博每日推文池（commit 3813c0d）
- 用户反馈今日推文推送《上海交大材料学院博士研究生“舵手计划”申请通知》，认为该文面向已入学博士生，与申博备考无关。
- 根因：该文被 `cat: "考博"` 错误分类，混进「考证/考博交替」的每日轮播池；tags 也被自动标为「认证体系」。
- 处理：在 `资料/articles.js` 中将 7 篇明显不适合每日轮播的文章从「考博」改为「资讯」：
  - 「舵手计划」→ 资讯/校内项目
  - 「博新计划」→ 资讯/博士后项目
  - 3 篇硕士招生/复试简章 → 资讯/硕士招生
  - 2 篇校史文章 → 资讯/校史
- 验证：puppeteer 检查 `DAILY_ARTICLES` pool，确认已无「舵手计划」「博新计划」「硕士研究生」等关键词；pool 从原 110 变为 103（过滤 overseas 后：考博 49→42，考证 61 不变）。
- 备注：当前备考计划 W1 从 2026-08-15 开始，今日（08-18）为第 4 天；用户需 Ctrl+F5 强制刷新以加载最新 `articles.js`。
